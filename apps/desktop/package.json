{
  "name": "@deepseek-ai/dsh-desktop",
  "description": "DSH Desktop shell: Electron window that supervises a local dsh web Host (MVP-A loopback)",
  "version": "0.1.0-rc.7",
  "private": true,
  "type": "module",
  "main": "lib/main.js",
  "files": [
    "lib",
    "electron-builder.yml"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Enffun-EL/deepseek-harness.git",
    "directory": "apps/desktop"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.preload.json --noEmit && node scripts/build-preload.mjs && node --input-type=module -e \"import { copyFileSync } from 'node:fs'; copyFileSync('src/dsh-desktop.d.ts', 'lib/dsh-desktop.d.ts'); copyFileSync('src/ipc-channels.json', 'lib/ipc-channels.json')\"",
    "dev": "pnpm run build && electron .",
    "start": "electron .",
    "smoke:host": "pnpm run build && node lib/smoke-host.js",
    "smoke:electron": "node ./scripts/smoke-electron.mjs",
    "test": "vitest run --config vitest.config.ts",
    "ensure-host-dist": "node ./scripts/ensure-host-dist.mjs",
    "pack": "pnpm run build && pnpm run ensure-host-dist && electron-builder --config electron-builder.yml --dir",
    "dist": "pnpm run build && pnpm run ensure-host-dist && electron-builder --config electron-builder.yml",
    "dist:win": "pnpm run build && pnpm run ensure-host-dist && electron-builder --config electron-builder.yml --win nsis portable",
    "dist:mac": "pnpm run build && pnpm run ensure-host-dist && electron-builder --config electron-builder.yml --mac dmg",
    "dist:linux": "pnpm run build && pnpm run ensure-host-dist && electron-builder --config electron-builder.yml --linux AppImage"
  },
  "dependencies": {
    "electron-updater": "^6.6.2",
    "tsx": "^4.20.3"
  },
  "devDependencies": {
    "electron": "^37.2.0",
    "electron-builder": "^24.13.3",
    "esbuild": "0.25.12",
    "typescript": "~5.9.2",
    "vitest": "^3.2.4"
  }
}
